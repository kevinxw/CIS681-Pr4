<?xml version="1.0"?>
<configuration>
  <appSettings>
    <!-- the address of server, changed it if you like.  But you need to change the address in server first -->
    <add key="SyncServiceAddress" value="net.tcp://localhost:5000/sync"/>
  </appSettings>
</configuration>
